<UserControl x:Class="Kandanda.Ui.Views.ActiveTournamentCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:ui="clr-namespace:Kandanda.Ui"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Content="Discard Plan" HorizontalAlignment="Left" Command="{Binding DiscardPlanCommand}" AutomationProperties.AutomationId="{x:Static ui:AutomationIds.ActiveTournamentDiscardButton}"/>
        <Button Grid.Column="1" Content="Publish" HorizontalAlignment="Right" Command="{Binding PublishCommand}" AutomationProperties.AutomationId="{x:Static ui:AutomationIds.ActiveTournamentPublishButton}"/>
        <Button Grid.Column="2" Content="Close" HorizontalAlignment="Right" Command="{Binding CloseCommand}" AutomationProperties.AutomationId="{x:Static ui:AutomationIds.ActiveTournamentCloseButton}"/>
    </Grid>
</UserControl>