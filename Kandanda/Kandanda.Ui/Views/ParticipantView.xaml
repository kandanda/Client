<UserControl x:Class="Kandanda.Ui.Views.ParticipantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"/>
                <DataGridTextColumn Header="Member Size"/>
                <DataGridTextColumn Header="Ranking"/>
                <DataGridComboBoxColumn Header="Group"/>
                <DataGridTextColumn Header="Delayed Arrival" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Content="Generate Plan" Command="{Binding GeneratePlanCommand}" 
                HorizontalAlignment="Right" VerticalAlignment="Bottom" />
    </Grid>
</UserControl>































































































































