<UserControl x:Class="Kandanda.Ui.Views.ParticipantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Participants"></Label>
        <Button Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Content="Generate Plan" Command="{Binding GeneratePlanCommand}" 
                HorizontalAlignment="Right" VerticalAlignment="Bottom"></Button>
    </Grid>
</UserControl>
