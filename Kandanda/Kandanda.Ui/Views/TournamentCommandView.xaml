<UserControl x:Class="Kandanda.Ui.Views.TournamentCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Content="Close" HorizontalAlignment="Left" Command="{Binding CloseCommand}"/>
        <Button Grid.Column="1" Content="Publish" HorizontalAlignment="Right" Command="{Binding PublishCommand}"/>
    </Grid>
</UserControl>